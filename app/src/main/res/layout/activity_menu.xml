<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/menu_principal"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:layout_margin="5dp"
        android:background="@drawable/img_asset_62hdpi"
        android:backgroundTint="#e8e4ddd3"
        android:backgroundTintMode="multiply"
        android:fitsSystemWindows="true"
        app:elevation="6dp"
        app:headerLayout="@layout/nav_header_menu"
        app:itemBackground="@drawable/asset_23_2x"
        app:itemIconTint="@color/white"
        app:itemTextAppearance="@android:style/TextAppearance.Holo.Large"
        app:itemTextColor="@color/white"
        app:menu="@menu/activity_menu_drawer" />

</android.support.v4.widget.DrawerLayout>
